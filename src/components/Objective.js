import React from "react";

const Objective = (props) => {
    return (
        <section>
            <h2>Objective</h2>
            <p>{props.objective}</p>
        </section>
    );
}

export default Objective;